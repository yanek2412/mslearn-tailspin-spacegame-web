export default class Switcher {
    private locator;
    constructor(parentLocator, currentLocator) {
        this.locator = `${parentLocator} ${currentLocator}`;
    }

    private isSwitcherOn() {
        
    }
    
    setOn() {

    }

    setOff() {

    }
}